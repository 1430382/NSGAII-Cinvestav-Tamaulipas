##33 Application of a multi-objective approach to solve the problem of phylogenetic reconstruction

#333
NSGA-II##version=0.1

En la carpeta de Initializer se encuentran los grd de varias instancias

tales como RBCL, ZILLA, MTDNA, etc.

En la carpeta de initializersBin se encuentran los grd, de el set uno y e#l set2(ANGI, CARP,TST01, etc).

En la carpeta de Instances se encuentran las instancias de los grd de

Initializers(RBCL,ZILLA,MTDNA,etc).

InstancesBin como su nombre indica contiene las instancias binarias de

InitializersBin(ANGI, CARP,TST01, etc).

En la carpeta de instanciasestadias se encuentra el set uno y set dos que ya

mencione anteriormente pero ya estan convertidas para que el algoritmo tr#abaje con ellas.

PhytoFasta es un codigo de python que convierte archivos phylip a fasta,

esto quedo inconcluso ya que el algoritmo no acepta fasta todavia por

problemas de lectura(el archivo execute.sh es el corregido).

En la carpeta sets se encuentran desde el set uno hasta el cuatro obtenidos de http://www.info.univ-angers.fr/~richer/recbio_phylo.php#DATA.

En la carpeta smallinstances vienen las 30 instancias alineadas(drosophyl2,mtDNA_186,rbCL_55,RDPI_218,ZILLA_500).
